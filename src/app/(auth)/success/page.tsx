import { SuccessView } from '@/features/auth';

export default function SuccessPage() {
  return (
    <div className="flex-1 flex items-center justify-center min-h-full w-full">
      <SuccessView />
    </div>
  );
}
